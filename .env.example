IMMICH_DB_PASSWORD=change_me_strong
PHOTOPRISM_ADMIN_PASSWORD=change_me_strong
PHOTOPRISM_DB_ROOT=change_me_strong
PHOTOPRISM_DB_PASS=change_me_strong
NAS_HOST=nas.local
NAS_MOUNT_POINT=/Volumes/Photos
NAS_NFS_EXPORT=/volume1/photos        # NFS export path on your NAS
# NFS_MOUNT_OPTIONS=vers=3,resvport,rw,hard,intr,tcp  # Optional overrides

#   BACKUP_DIR            # Target backup path (default: /Volumes/Photos/backups)
#   RETENTION_DAYS        # How many days to keep old backups (default: 14)

# --- Immich ---

# You can find documentation for all the supported env variables at https://docs.immich.app/install/environment-variables

# The location where your uploaded files are stored
UPLOAD_LOCATION=/Volumes/Photos/uploads/from-immich

# The location where your database files are stored. Network shares are not supported for the database
# !! Should be a local SSD
DB_DATA_LOCATION=~/immich/postgres

# To set a timezone, uncomment the next line and change Etc/UTC to a TZ identifier from this list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List
TZ=America/New_York

# The Immich version to use. You can pin this to a specific version like "v2.1.0"
IMMICH_VERSION=v2

# Connection secret for postgres. You should change it to a random password
# Please use only the characters `A-Za-z0-9`, without special characters or spaces
DB_PASSWORD=changemestrong


# The values below this line do not need to be changed
###################################################################################
DB_USERNAME=postgres
DB_DATABASE_NAME=immich

# --- Tailscale / Caddy ---
TS_NODE=mac.tailnet.ts.net

# Public web domain(s)
PUBLIC_DOMAIN=photos.example.com
IMMICH_PUBLIC_DOMAIN=immich.example.com
PHOTOPRISM_PUBLIC_DOMAIN=prism.example.com

# Where Caddy certs will live on the host (mounted into container read-only)
CADDY_CERTS_DIR="$HOME/caddy/certs"

# Where the Caddyfile will live on the host (mounted into container read-only)
CADDYFILE_TARGET="$HOME/caddy/Caddyfile"

# Docker container name & ports
CADDY_CONTAINER_NAME=caddy
CADDY_HTTP_PORT=80
CADDY_HTTPS_PORT=443

# If they’re on another tailnet node (i.e. PP on NAS), set:
# IMMICH_UPSTREAM=mac.tailnet.ts.net:2283 (or that node’s name)
# PHOTOPRISM_UPSTREAM=othernode.tailnet.ts.net:2342
IMMICH_UPSTREAM=immich_server:2283
PHOTOPRISM_UPSTREAM=photoprism:2342
